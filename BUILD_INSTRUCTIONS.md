# BUILD INSTRUCTIONS: change-order-tracker-field-service-contractors

## Overview

- **App Name:** change-order-tracker-field-service-contractors
- **Repository:** change-order-tracker-field-service-contractors
- **Tech Stack:** Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- **Description:** change-order-tracker-field-service-contractors

---

## Tech Stack & Setup

### Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui

```bash
npx create-next-app@latest change-order-tracker-field-service-contractors --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
cd change-order-tracker-field-service-contractors
# shadcn/ui (always include)
npx shadcn@latest init -d
npx shadcn@latest add button card input label form toast badge
# Only add Prisma if the PRD requires data persistence:
# npm install prisma @prisma/client
# npx prisma init
# Only add NextAuth if the PRD requires user login:
# npm install next-auth@beta @auth/prisma-adapter bcryptjs
# npm install -D @types/bcryptjs
# Only add Stripe if the PRD explicitly requires payments:
# npm install stripe @stripe/stripe-js
# Only add Resend if the PRD explicitly requires email:
# npm install resend
```

### Configuration

Add to next.config.ts:
```ts
const nextConfig = {
  output: "standalone",
};
export default nextConfig;
```

### Development

```bash
npm run dev
```

### Build

```bash
npm run build
```

---

## Product Requirements Document

The full PRD is included below. Implement ALL features described.

---

# Change Order Tracker for Field Service Contractors

*Generated by Forge on 2026-05-01T08:11:10.891Z — score 8.1/10*

## Overview

**Change Order Tracker for Field Service Contractors** is a **web SaaS** for small plumbing shops, AV installers, and similar field-service contractors who lose margin when onsite scope changes are handled through texts, verbal approvals, or scattered PDFs. The product lets techs or owner-operators capture a change request in the field, attach photos and labor/material impact, send a simple approval link to the client, and generate invoice-ready documentation for the office. The wedge is not “another CRM” or “another invoicing app,” but a purpose-built scope-change approval workflow that is faster and easier than broad field service platforms.

## Why this will succeed

This opportunity has credible validation, but it only works if positioned narrowly around **scope-change capture + approval + documentation** rather than trying to compete head-on as general plumber CRM or invoicing software.

### Comparable products that are working

1. **Jobber**  
   URL: https://getjobber.com/  
   Size: Large field service SaaS; widely known incumbent in home services  
   What works: Jobber proves that small service businesses pay recurring subscription fees for operational workflow software tied to quoting, jobs, and invoicing.

2. **Housecall Pro**  
   URL: https://www.housecallpro.com/  
   Size: Large field service platform serving thousands of service businesses  
   What works: Housecall Pro validates that owner-operators and small teams will adopt software that improves field-to-office workflow and customer approval processes.

3. **Buildertrend**  
   URL: https://www.buildertrend.com/  
   Size: Established construction/project management software with change order features  
   What works: Buildertrend proves that change orders themselves are a monetizable workflow category, especially when tied to project documentation and approvals.

### Specific data points from research

- Reachable market estimate from the research: **3,000–8,000 reachable users** by targeting owner-operators and office managers at small plumbing shops in the US/Canada.
- First-customer channels named in research: **r/Plumbing**, **Plumbing Zone**, **ServiceTitan Community**, and **AV/integration forums**.
- The keyword set is high intent and narrowly aligned to the problem:
  - “av installer change order software”
  - “commercial audiovisual scope change tracker”
  - “integration project change request log”
  - “change order”
  - “field service”
  - “client approval”
  - “scope changes”
  - “job documentation”
- Research explicitly notes this opportunity was surfaced by **two independent research sources: Hacker News and autocomplete**, which is a better signal than a single anecdotal source.
- The financial pain is concrete: **unapproved field changes cause margin leakage, billing disputes, and delayed invoicing**.

### Why this specific opportunity is different / better

The gap is not that incumbents lack all relevant features; it is that they are too broad for this specific job.

- **Jobber** and **Housecall Pro** are optimized around broad FSM workflows like scheduling, dispatch, and invoicing, not a fast field-first “extra work happened, document it, price it, get approval now” flow.
- **Buildertrend** supports change orders, but research indicates it is heavier and more construction-management-oriented than what a small plumbing or AV install shop wants for quick field capture.
- The opportunity is to win on:
  - **Speed in the field**
  - **Simplicity on mobile web**
  - **Invoice-ready documentation**
  - **Client approval before work proceeds**
  - **Narrow fit for owner-operators and small teams**

This is strong enough for a bootstrapped niche SaaS if the product stays focused. It is not strong enough if expanded into generic CRM, generic quoting, or generic invoicing from day one.

## How this will succeed

### Primary channel

**SEO**

SEO is the best primary channel because the research already identified a set of **high-intent long-tail keywords** around change orders and scope changes, and these are narrower and less contested than broad terms like “plumber CRM software.” The product also has a strong documentation/template angle that naturally supports landing pages, comparison pages, and educational content.

**Secondary channel:** community seeding in r/Plumbing, Plumbing Zone, ServiceTitan Community, and AV/integration forums to validate messaging and drive the first demos while SEO compounds.

### Channel-specific playbook

Because the research did not provide verified search volumes or live SERP snapshots, this plan uses the exact researched keyword clusters and requires manual SERP validation in week 1 before content production. Do not invent volume numbers on the site or in planning.

#### Exact keyword cluster

Primary commercial-intent cluster:
- change order software for plumbers
- plumber change order app
- field service change order software
- scope change approval app
- client approval for extra work
- change order tracker for contractors
- onsite change request software
- invoice-ready change order documentation

AV/integration cluster:
- av installer change order software
- commercial audiovisual scope change tracker
- integration project change request log

Template / educational cluster:
- field service change order template
- plumbing extra work approval form
- change order form for contractors
- how to document scope changes onsite
- avoid billing disputes for extra work

#### Current SERP competition

The research names broad incumbents likely to dominate adjacent SERPs:
- Jobber
- Housecall Pro
- Buildertrend

A new site is unlikely to rank quickly for broad software terms like:
- plumber software
- crm software for plumbers
- invoicing software for plumbers

A new site has a realistic chance at narrower terms and template-driven searches if pages are highly specific and paired with direct outreach:
- av installer change order software
- scope change approval app
- field service change order template
- plumbing extra work approval form
- client approval for extra work

#### Can a new site break in?

Yes, for the narrowest pain-point terms and template-led pages. No, not quickly for broad FSM categories. The SEO plan should therefore avoid top-of-funnel vanity keywords and focus on:
1. bottom-of-funnel software pages,
2. template pages,
3. comparison pages,
4. use-case pages by trade.

#### First 10 pages to build

1. Homepage: “Change Order Software for Field Service Contractors”
2. /plumbers/change-order-software
3. /av-installers/change-order-software
4. /field-service/change-order-template
5. /client-approval/extra-work
6. /compare/jobber-vs-change-order-tracker
7. /compare/housecall-pro-vs-change-order-tracker
8. /compare/buildertrend-vs-change-order-tracker
9. /use-cases/invoice-ready-job-documentation
10. /use-cases/onsite-scope-change-approval

### Week-by-week execution plan

#### Week 4 (beta + warm-up)

- Publish the first 5 SEO pages:
  - homepage
  - plumbers page
  - AV installers page
  - template page
  - client approval page
- Create 2 comparison pages:
  - Jobber alternative for change orders
  - Housecall Pro alternative for extra work approvals
- Build outreach spreadsheet with:
  - 50 plumbing shops
  - 30 AV/integration contractors
  - 20 office managers / estimators / owners found via company websites and LinkedIn
- Warm community accounts by posting useful, non-promotional content for at least 2 weeks:
  - comment on workflow/paperwork discussions
  - share a free change order template
  - ask how shops currently handle extra work approvals
- Set up analytics:
  - Google Search Console
  - Plausible or PostHog
  - conversion events for demo request, signup, approval sent, paid upgrade

#### Week 5 (launch)

**Monday**
- Publish launch post on company site: “Why Small Contractors Lose Money on Unapproved Extra Work”
- Email first 20 warm prospects with offer for free setup and feedback
- Send personal DMs/emails to prior interviewees

**Tuesday**
- Post value-first thread in r/Plumbing  
  Suggested title: **“Free change order template for extra work approvals — what are you all using now?”**  
  Goal: conversation first, product mention only if asked or allowed.

**Wednesday**
- Post in Plumbing Zone or equivalent forum  
  Suggested title: **“How do small shops document onsite scope changes before billing?”**
- Share downloadable template and checklist, not homepage CTA first.

**Thursday**
- Post in AV/integration community  
  Suggested title: **“How do you handle client signoff when install scope changes mid-job?”**
- Link to AV-specific landing page only after discussion context is established.

**Friday**
- Send second outreach batch to 20 more shops with a tailored note:
  - “Saw you do residential/commercial installs”
  - “We built a lightweight change-order approval tool for field crews”
  - “Happy to set up your logo/template free for beta users”

**Saturday**
- Publish comparison page targeting “Jobber alternative for change orders”
- Follow up with all community replies; capture exact wording users use.

**Sunday**
- Review analytics:
  - signup rate by source
  - page-level CTR from search impressions if any
  - outreach response rate
  - which vertical responds better: plumbing or AV

#### Weeks 6-8 (compound)

Repeats weekly:
- Publish 2 SEO pages per week:
  - one use-case page
  - one comparison or template page
- Reach out to 15 new prospects per week
- Post or comment value-first in 2 communities per week
- Conduct 3 customer calls per week
- Refine homepage copy using exact phrases from calls

One-time initiatives:
- Add one downloadable resource:
  - plumbing change order template PDF
  - AV scope-change checklist
- Create one short demo video embedded on landing pages

If SEO underperforms by week 6:
- Shift effort toward direct outreach and community-driven discovery calls
- Double down on template pages and comparison pages rather than broad software pages
- Narrow the positioning to whichever vertical has higher reply and activation rates: plumbing or AV

#### 3-month targets

- 10 published SEO pages by end of month 2, 18+ by end of month 3
- 30–50 qualified conversations from outreach + communities
- 15–25 trial accounts
- 5–12 paying customers
- First page indexing for long-tail change-order terms by month 2
- Top 20 ranking for at least 3 long-tail pages by month 3
- Clear signal on best vertical: plumbing vs AV based on trial-to-paid conversion

### Budget & CAC

**3-month cash budget:** $2,500–$7,500  
Grounded in research estimate.

Suggested allocation:
- $0–$500: forum sponsorships / small community tests if available
- $300–$900: SEO tooling, analytics, domain, email
- $500–$1,500: design/help for 1-2 high-quality template resources or explainer video
- $1,200–$4,600: founder-led prospecting support, list building, and optional niche sponsorship tests

**Founder time:** 12–18 hours/week for 12 weeks  
Approx. 145–215 hours total across outreach, content, interviews, and support.

**Expected CAC:** $80–$250  
Grounded in the research estimate and realistic for founder-led outbound plus niche SEO in a specialized B2B workflow category.

### Why this works at zero/low cost

The playbook works without an audience because it targets **specific high-intent searches** and **named contractor communities** where the exact pain already exists. The founder’s main asset is not ad spend; it is the ability to ship narrow landing pages, free templates, and direct outreach quickly.

## Target user

**Primary persona:** owner-operator or office manager at a small plumbing shop in the US/Canada, typically with a handful of field techs and no appetite for a heavy all-in-one platform rollout.

- **Who they are:** small field-service contractor handling residential or light commercial jobs, often with the owner still involved in quoting, approvals, billing, or dispute resolution.
- **What they currently do:** text the customer, note changes in the invoice later, scribble changes in job notes, use PDFs inconsistently, or rely on memory and verbal approval.
- **Why current options fail them:** broad tools like Jobber or Housecall Pro solve many workflows but do not make change-order capture and pre-approval the central flow; construction tools like Buildertrend are too heavy for fast field use.
- **What they care about:** protecting margin, reducing disputes, proving what changed, and billing faster.
- **Where they already hang out online:** r/Plumbing, Plumbing Zone, ServiceTitan Community; secondarily, AV/integration forums for the commercial installer segment.

## Core features (MVP)

- **Company/workspace setup**
  - business name
  - logo
  - default terms
  - tax rate
  - currency
- **Jobs**
  - create job with customer name, site address, original scope summary
- **Change orders**
  - create change request from a job
  - title + description of scope change
  - labor impact
  - material impact
  - total price impact
  - urgency flag
- **Photo/document attachments**
  - upload before/after or issue photos
  - attach one PDF or image set per change order
- **Approval link**
  - generate secure client-facing approval page
  - accept typed signature/name
  - approve or reject with timestamp
- **Status tracking**
  - draft
  - sent
  - viewed
  - approved
  - rejected
  - invoiced
- **PDF generation**
  - branded PDF with job info, scope change, price impact, photos, and approval status
- **Activity log**
  - timestamped log of who created, sent, viewed, approved
- **Simple team roles**
  - owner/admin
  - field user
- **Dashboard**
  - open change orders
  - awaiting approval
  - approved not invoiced
  - rejected
- **Email sending**
  - send approval request via email
  - resend reminder
- **Basic exports**
  - CSV export of change orders
  - PDF export per change order

## Tech stack recommendation

Use the standard web SaaS stack.

- **Next.js 15 (App Router) + TypeScript**
- **PostgreSQL + Prisma ORM**
- **Tailwind CSS + shadcn/ui**
- **NextAuth v5 with Google OAuth**
- **Stripe** for subscriptions
- **Resend** for approval-request and transactional emails
- **Coolify** deployment with Docker
- **PWA-friendly responsive web app** for mobile browser use in the field

No native mobile stack should be used. This workflow fits a responsive mobile-web SaaS well because office staff and field crews need shared records and approval links, not app-store-dependent native features.

## Monetization & pricing

**Primary strategy: subscription**

This matches how comparable operational SaaS products monetize and aligns with recurring workflow value. The value is not “a form tool”; it is margin protection and faster billing.

### Suggested pricing

- **Solo — $39/month**
  - 1 user
  - unlimited jobs
  - up to 30 change orders/month
  - approval links
  - PDF exports

- **Small Team — $79/month**
  - up to 3 users
  - unlimited change orders
  - branded PDFs
  - email reminders
  - activity log

- **Office — $149/month**
  - up to 8 users
  - admin controls
  - CSV exports
  - priority support
  - custom approval terms
  - multiple field users + office workflow

- **Additional users:** $15/month each on Office if needed later

### Why this pricing works

Research already supports pricing in this range:
- incumbents like Jobber, Housecall Pro, and Buildertrend validate that businesses in this category already pay meaningful monthly software spend;
- the product protects revenue leakage, so pricing should reflect financial ROI rather than commodity utility pricing.

A free plan is not necessary at launch. A **14-day trial** is enough.

## Keyword / SEO / ASO strategy

This is a web SaaS, so focus on SEO.

### Primary keywords

Because the research did not provide verified volumes, treat these as target terms to validate in Ahrefs/SEM rush/Google search manually:

- change order software for plumbers
- plumber change order app
- field service change order software
- scope change approval app
- client approval for extra work
- av installer change order software
- commercial audiovisual scope change tracker
- integration project change request log

### Long-tail targets

- field service change order template
- plumbing extra work approval form
- contractor change order documentation
- how to approve extra work onsite
- invoice-ready change order form
- avoid billing disputes for extra work
- document scope creep for service jobs

### Meta title format

`[Primary Keyword] | Fast Client Approval + Invoice-Ready Docs`

Examples:
- `Change Order Software for Plumbers | Fast Client Approval + Invoice-Ready Docs`
- `AV Installer Change Order Software | Capture Scope Changes Onsite`

### Meta description format

`Capture onsite scope changes, send approval links to clients, and generate invoice-ready documentation without heavy FSM software.`

### Site structure

- /
- /plumbers/change-order-software
- /av-installers/change-order-software
- /templates/[slug]
- /compare/[slug]
- /use-cases/[slug]
- /blog/[slug]
- /pricing
- /demo
- /login

### Pages to prioritize

1. Homepage
2. Plumber-specific page
3. AV-specific page
4. Change order template page
5. Client approval page
6. Jobber comparison
7. Housecall Pro comparison
8. Buildertrend comparison

## Revenue model

This model is tied to founder-led SEO + outreach, not mass-market traffic.

### Funnel assumptions

- Visitor to trial/signup: 2%–5% on high-intent pages
- Trial to active use: 40%–60%
- Active to paid: 25%–40%
- Outreach reply rate: 5%–15%
- Demo-to-trial rate: 30%–50%

### Month 1

- 150–300 site visitors
- 20–30 outreach conversations
- 5–8 trials
- 1–3 paid customers

Revenue:
- If 2 customers at blended ARPU of $59 = **$118 MRR**
- If 3 customers at blended ARPU of $79 = **$237 MRR**

### Month 3

- 800–1,500 site visitors
- 30–50 qualified conversations total
- 15–25 trials cumulative
- 5–12 paid customers

Revenue:
- 5 customers at $59 blended ARPU = **$295 MRR**
- 12 customers at $79 blended ARPU = **$948 MRR**

### Month 6

- 2,000–4,000 site visitors
- 35–60 trials cumulative
- 15–35 paid customers

Revenue:
- 15 customers at $69 blended ARPU = **$1,035 MRR**
- 35 customers at $89 blended ARPU = **$3,115 MRR**

### Month 12

This should be anchored to the research projection.

- 40–90 paid customers
- blended ARPU: $75–$100 depending on mix

Revenue:
- 40 customers at $75 = **$3,000 MRR**
- 60 customers at $100 = **$6,000 MRR**
- upside case consistent with research: **$18,000–$35,000 MRR** would require stronger penetration, better team-plan mix, and broader adoption than the base case

## Competitor landscape

### Jobber
- URL: https://getjobber.com/
- Pricing model: subscription SaaS
- Key features: broad field service management including scheduling, jobs, quoting, invoicing
- Specific gaps/weaknesses: broad FSM platform rather than purpose-built change-order approval workflow; can be overkill and relatively expensive for owner-operators who mainly need scope-change capture and signoff
- Our differentiation: faster field capture, approval-first workflow, invoice-ready change-order documentation, narrower setup

### Housecall Pro
- URL: https://www.housecallpro.com/
- Pricing model: subscription SaaS
- Key features: scheduling, dispatch, payments, customer management, invoicing
- Specific gaps/weaknesses: optimized around general service operations more than disputed scope-change documentation; generic workflow leaves room for a simpler jobsite approval experience
- Our differentiation: centered on extra work approvals, proof, timestamps, and office-ready documentation rather than general FSM breadth

### Buildertrend
- URL: https://www.buildertrend.com/
- Pricing model: subscription SaaS
- Key features: construction/project management with change order support
- Specific gaps/weaknesses: geared toward broader construction management; heavier implementation burden for small AV installers or plumbing shops needing fast field capture
- Our differentiation: lightweight field-service-first UX, minimal setup, mobile-web approval flow, no heavy PM overhead

## Timeline

### Week 1-2: core MVP

**Week 1**
- Set up Next.js app, auth, database, shadcn UI
- Build data models for users, workspaces, jobs, change orders
- Implement dashboard shell and workspace onboarding
- Build create/edit job flow
- Build create/edit change order form

**Week 2**
- Add price impact fields and status model
- Build client-facing approval page with secure token
- Add email sending for approval requests
- Build activity log
- Add basic mobile-responsive layouts for field usage

### Week 3: polish + testing

- Implement PDF generation
- Add file uploads for photos/documents
- Add dashboard filters
- Add resend reminder flow
- QA approval lifecycle end-to-end
- Seed sample/demo data

### Week 4: final build work while marketing prep happens in parallel

- Add pricing page and Stripe checkout
- Add trial gating and subscription checks
- Add CSV export
- Improve onboarding
- Add analytics events
- Fix mobile UX issues from testing

### Week 5: launch

- Deploy production version
- Enable billing
- Open beta / trial access
- Monitor errors, support early users, and patch friction points quickly

## Score breakdown

- **recurringRevenue: 8/10** — recurring workflow pain with clear monthly value and natural subscription fit.
- **buildComplexity: 7/10** — straightforward SaaS architecture, but includes approvals, PDF generation, uploads, and subscriptions.
- **marketValidation: 7/10** — validated by real incumbent products and concrete workflow pain, though niche demand still needs direct customer confirmation.
- **competition: 5/10** — meaningful competition from broad FSM and construction software; focus is required to avoid feature wars.
- **automationPotential: 8/10** — strong potential for reminders, documentation generation, status tracking, and office handoff automation.
- **platformFit: 9/10** — web SaaS is an excellent fit because field and office need shared access, approvals, and exports.
- **distributionFeasibility: 8/10** — strongest score driver is named, reachable communities plus long-tail high-intent SEO around change-order and scope-change terms.

## Build instructions for the coding agent

1. Initialize project:  
   `npx create-next-app@latest change-order-tracker-field-service-contractors --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`

2. Install dependencies:  
   `npm install prisma @prisma/client next-auth @auth/prisma-adapter stripe resend zod react-hook-form @hookform/resolvers date-fns uploadthing pdf-lib zustand`  
   Also install any required uploadthing/Next.js adapter packages.

3. Install shadcn/ui:  
   `npx shadcn@latest init`  
   Add components: button, input, textarea, card, dialog, form, table, badge, select, dropdown-menu, tabs, toast, sheet, alert-dialog.

4. Set up Prisma:  
   `npx prisma init`  
   Configure PostgreSQL in `.env`.

5. Define Prisma schema in `prisma/schema.prisma` with these models:
   - `User`
   - `Account`
   - `Session`
   - `VerificationToken`
   - `Workspace`
   - `Membership`
   - `Customer`
   - `Job`
   - `ChangeOrder`
   - `ChangeOrderAttachment`
   - `ApprovalRequest`
   - `ApprovalEvent`
   - `Subscription`

6. Use these core data shapes:
   - `Workspace`: `id`, `name`, `slug`, `logoUrl`, `defaultTerms`, `taxRate`, `currency`, `createdAt`
   - `Job`: `id`, `workspaceId`, `customerName`, `customerEmail`, `siteAddress`, `originalScope`, `status`, `createdById`
   - `ChangeOrder`: `id`, `jobId`, `workspaceId`, `title`, `description`, `laborImpactCents`, `materialImpactCents`, `otherImpactCents`, `totalCents`, `status`, `urgency`, `sentAt`, `approvedAt`, `rejectedAt`, `invoicedAt`, `createdById`
   - `ApprovalRequest`: `id`, `changeOrderId`, `token`, `recipientName`, `recipientEmail`, `message`, `expiresAt`, `viewedAt`, `approvedAt`, `rejectedAt`, `signerName`, `signerIp`
   - `ApprovalEvent`: `id`, `changeOrderId`, `type`, `actorType`, `actorUserId`, `metadataJson`, `createdAt`

7. Run migration and generate client:  
   `npx prisma migrate dev --name init`  
   `npx prisma generate`

8. Set up NextAuth v5 in `src/auth.ts` and `src/app/api/auth/[...nextauth]/route.ts` with Google OAuth and Prisma adapter. Restrict app routes to authenticated users except approval links and marketing pages.

9. Create app route groups:
   - `src/app/(marketing)/page.tsx`
   - `src/app/(marketing)/pricing/page.tsx`
   - `src/app/(marketing)/plumbers/change-order-software/page.tsx`
   - `src/app/(marketing)/av-installers/change-order-software/page.tsx`
   - `src/app/(app)/dashboard/page.tsx`
   - `src/app/(app)/jobs/page.tsx`
   - `src/app/(app)/jobs/[jobId]/page.tsx`
   - `src/app/(app)/change-orders/[id]/page.tsx`
   - `src/app/approve/[token]/page.tsx`

10. Build workspace onboarding flow:
   - `src/components/onboarding/workspace-onboarding-form.tsx`
   - Fields: company name, logo URL/upload, tax rate, currency, default approval terms
   - Redirect new users to onboarding before dashboard access

11. Build job CRUD:
   - `src/components/jobs/job-form.tsx`
   - `src/app/(app)/jobs/new/page.tsx`
   - Support customer name, email, site address, original scope
   - List jobs on `/jobs` with simple search/filter

12. Build change order CRUD:
   - `src/components/change-orders/change-order-form.tsx`
   - Include title, description, labor/material/other impact, urgency, attachments
   - Auto-calculate total
   - Allow creating from a job detail page

13. Implement status system with enum values:
   - `DRAFT`
   - `SENT`
   - `VIEWED`
   - `APPROVED`
   - `REJECTED`
   - `INVOICED`
   Show badges consistently in tables and detail pages.

14. Add file uploads for attachments:
   - `src/lib/uploads.ts`
   - Store attachment metadata in `ChangeOrderAttachment`
   - Allow image and PDF uploads only
   - Display thumbnails/previews where possible

15. Implement approval request flow:
   - Create tokenized approval request records
   - Public route `/approve/[token]` shows scope, pricing, photos, and terms
   - Buttons: Approve / Reject
   - Collect typed signer name
   - Save IP from request headers if available
   - Log `VIEWED`, `APPROVED`, `REJECTED` in `ApprovalEvent`

16. Set up Resend email sending:
   - `src/lib/email.ts`
   - Send approval request email with CTA link
   - Send reminder email
   - Use branded workspace name in subject:
     - `Approval needed for extra work at [site address]`

17. Generate printable PDF:
   - `src/lib/pdf/change-order-pdf.ts`
   - Include workspace logo/name, customer/job info, scope change details, cost breakdown, attachments list, approval state, timestamps, signer name
   - Add “Download PDF” action on change order detail page

18. Add Stripe subscriptions:
   - Create pricing page in `src/app/(marketing)/pricing/page.tsx`
   - Add checkout button components
   - Create webhook route `src/app/api/webhooks/stripe/route.ts`
   - Store subscription status in `Subscription`
   - Gate access by trial/subscription state, but allow limited onboarding before paywall

19. Build dashboard and reporting:
   - `src/app/(app)/dashboard/page.tsx`
   - KPI cards: awaiting approval, approved not invoiced, rejected, total this month
   - Recent change orders table
   - Add CSV export action from dashboard or change orders list

20. Add analytics, QA, and deployment:
   - Track events: signup, workspace_created, job_created, change_order_sent, approval_viewed, approval_approved, subscription_started
   - Verify mobile responsiveness on iPhone/Android viewport sizes
   - Add `Dockerfile` and Next.js standalone output in `next.config.ts`
   - Confirm `npm run build` passes
   - Deploy to Coolify with environment variables for DB, Auth, Stripe, Resend, uploads
   - Seed demo workspace and sample change orders for screenshots and onboarding walkthrough


---

## Dockerfile

```dockerfile
# ============================================================
# BASE DOCKERFILE — for apps WITHOUT a database
# If this app uses Prisma/SQLite, see the "With Prisma" section below
# ============================================================

# Stage 1: Dependencies
FROM node:20-slim AS deps
WORKDIR /app
COPY package.json package-lock.json* ./
# --ignore-scripts prevents postinstall hooks (e.g. prisma generate) from running
# here where schema.prisma isn't available yet. Scripts run in the builder stage.
RUN npm ci --ignore-scripts

# Stage 2: Build
FROM node:20-slim AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
# Provide build-time defaults so the build succeeds with no env vars configured
ENV AUTH_SECRET="build-time-placeholder-secret"
ENV NEXT_PUBLIC_APP_URL="https://localhost:3000"
# Run any postinstall scripts now that all source files are present
RUN npm rebuild 2>/dev/null || true
RUN npm run build

# Stage 3: Runner
FROM node:20-slim AS runner
WORKDIR /app
ENV NODE_ENV=production
# Default AUTH_SECRET — app works with no Coolify env vars; override for production
ENV AUTH_SECRET="forge-app-default-secret-override-in-production"
ENV NEXT_PUBLIC_APP_URL=""

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs
EXPOSE 3000
ENV PORT=3000
CMD ["node", "server.js"]

# ============================================================
# WITH PRISMA/SQLITE — replace the above Dockerfile entirely
# Use this when the PRD requires data persistence or user auth
# ============================================================
#
# FROM node:20-slim AS deps
# WORKDIR /app
# COPY package.json package-lock.json* ./
# # --ignore-scripts prevents prisma generate from running before schema.prisma is copied
# RUN npm ci --ignore-scripts
#
# FROM node:20-slim AS builder
# # Install OpenSSL for Prisma schema engine (debian-openssl-3.0.x target)
# RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*
# WORKDIR /app
# COPY --from=deps /app/node_modules ./node_modules
# COPY . .
# ENV DATABASE_URL="file:/tmp/build.db"
# ENV AUTH_SECRET="build-time-placeholder-secret"
# ENV NEXT_PUBLIC_APP_URL="https://localhost:3000"
# # Generate Prisma client now that schema.prisma is available
# RUN npx prisma generate
# RUN npm run build
#
# FROM node:20-slim AS runner
# # Install OpenSSL for Prisma schema engine at runtime
# RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*
# WORKDIR /app
# ENV NODE_ENV=production
# ENV DATABASE_URL="file:/data/app.db"
# ENV AUTH_SECRET="forge-app-default-secret-override-in-production"
# ENV NEXT_PUBLIC_APP_URL=""
# RUN addgroup --system --gid 1001 nodejs
# RUN adduser --system --uid 1001 nextjs
# RUN mkdir -p /data && chown nextjs:nodejs /data
# COPY --from=builder /app/public ./public
# COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
# COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
# COPY --from=builder --chown=nextjs:nodejs /app/prisma ./prisma
# # Copy full node_modules so the Prisma CLI has all its runtime deps (v6+ requires effect, c12, etc.)
# COPY --from=builder --chown=nextjs:nodejs /app/node_modules ./node_modules
# USER nextjs
# EXPOSE 3000
# ENV PORT=3000
# CMD ["sh", "-c", "node node_modules/prisma/build/index.js db push --skip-generate && echo 'DB schema initialized' && node server.js"]
```

---

## File Organization

- Keep source code in `src/` (if applicable)
- Components in `src/components/`
- Utilities in `src/lib/` or `src/utils/`
- API routes in `src/app/api/` (Next.js) or equivalent
- Static assets in `public/`
- Environment variables in `.env.local` (never commit secrets)

---

## Build Verification

Run `npm run build` and ensure zero errors before committing.

- No TypeScript errors
- No ESLint warnings (fix or disable with justification)
- All pages/screens render without crashes
- API routes return expected responses

---

## Git Instructions

When the build is complete and verified:

1. Initialize the repository if not already done: `git init`
2. Stage all files: `git add -A`
3. Commit: `git commit -m "Initial build: change-order-tracker-field-service-contractors"`
4. Push to remote when instructed

---

## Required Files Checklist

- [ ] `README.md` - Project description, setup instructions, tech stack
- [ ] `.gitignore` - Appropriate for Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui
- [ ] `.env.example` - Template for required environment variables (see below)
- [ ] `Dockerfile` - Production-ready container build
- [ ] `package.json` (or equivalent) - Dependencies and scripts

---

## Zero-Config Deployment Requirement

**CRITICAL: This app MUST start and run correctly with NO environment variables configured in the deployment platform (Coolify). All external service dependencies must either be absent or have safe fallback defaults baked in.**

Apps that crash on startup due to missing env vars will fail every deployment cycle.

**CRITICAL: Never instantiate third-party API clients at module level.** Next.js evaluates top-level code during `next build` — a `new Resend(...)`, `new Stripe(...)`, or `new OpenAI(...)` call at module scope will crash the build if the API key env var is missing. Always initialize these clients **inside** the handler/function body, guarded by an env var check.

---

## Environment Variables (.env.example)

Only include variables the app actually uses. If the PRD doesn't require a feature, don't add those variables.

```bash
# App URL (optional — remove if not needed)
NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# --- Only include below if PRD requires data persistence ---
# Database (SQLite — no external service required)
DATABASE_URL="file:./dev.db"

# --- Only include below if PRD requires user login ---
# NextAuth secret — a default is baked into the Dockerfile; override here for production
AUTH_SECRET="your-secret-here"  # generate with: openssl rand -base64 32

# --- Only include below if PRD explicitly requires payments ---
# STRIPE_SECRET_KEY="sk_live_..."
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Only include below if PRD explicitly requires email ---
# RESEND_API_KEY="re_..."
# EMAIL_FROM="noreply@yourdomain.com"
```

---

## Key Implementation Notes

### Database (only if PRD requires persistence)
- Use **SQLite** via Prisma: `provider = "sqlite"`, `url = env("DATABASE_URL")`
- `DATABASE_URL` defaults to `file:/data/app.db` in the Dockerfile — no Coolify config needed
- For local dev: `DATABASE_URL="file:./dev.db"` in `.env.local`
- Run `npx prisma db push` after schema changes
- **Do NOT use PostgreSQL** — no PostgreSQL server is provisioned in the deployment environment

### Authentication (only if PRD requires user login)
- Use **NextAuth v5** with the **Credentials provider** (email + password)
- **Do NOT use Google OAuth** — no OAuth credentials are configured in deployment
- Hash passwords with **bcrypt** (`bcryptjs` package)
- Session strategy: **`"jwt"`** (not `"database"`) — required for Credentials provider
- `AUTH_SECRET` defaults are baked into the Dockerfile; app works out of the box

### Payments (only if PRD explicitly requires it)
- Use Stripe only when the PRD's core functionality requires payment processing
- If adding Stripe: handle `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted` at `/api/webhooks/stripe`
- Make the app functional without Stripe credentials (show upgrade prompts rather than crashing)

### Email (only if PRD explicitly requires it)
- Use Resend only when the PRD requires transactional email
- **CRITICAL: NEVER instantiate `new Resend(...)` at module level** — always inside the handler function body with a guard:
  ```ts
  // WRONG — causes build failure (next build evaluates module-level code):
  const resend = new Resend(process.env.RESEND_API_KEY);
  export async function POST(req) { ... }

  // CORRECT — lazy init inside handler:
  export async function POST(req) {
    if (!process.env.RESEND_API_KEY) {
      console.log("[email] RESEND_API_KEY not set, skipping email");
      return NextResponse.json({ ok: true });
    }
    const { Resend } = await import("resend");
    const resend = new Resend(process.env.RESEND_API_KEY);
    ...
  }
  ```
- Next.js evaluates module-level code during `next build` page data collection — missing env vars at module scope crash the build
- Same rule applies to Stripe, OpenAI, and ALL third-party SDK clients — always lazy-initialize inside handler functions

### Deployment
- Coolify with Docker — always set `output: "standalone"` in next.config.ts
- Do NOT add `COPY public ./public` in the Dockerfile if the public/ directory is empty or doesn't exist
- The Dockerfile's CMD initializes the SQLite database on first start via `prisma db push`
- App must respond healthy on port 3000 within 60 seconds of container start
- **CRITICAL Dockerfile rule**: In the `deps` stage, always use `RUN npm ci --ignore-scripts`. Without `--ignore-scripts`, the `postinstall` hook (e.g. `prisma generate`) runs before `prisma/schema.prisma` is copied, causing the build to fail. Run `npx prisma generate` explicitly in the `builder` stage after `COPY . .`.
- **Do NOT run `npx prisma db push` in the Dockerfile builder stage** — it is not needed at build time. The CMD already runs `prisma db push` at container startup when a real `/data/app.db` path is available.
- **Use `node:20-slim` (not `node:20-alpine`)** — Debian slim avoids Alpine musl/libssl issues. Add `RUN apt-get update -y && apt-get install -y openssl && rm -rf /var/lib/apt/lists/*` to BOTH the builder and runner stages for Prisma's schema engine.
- **Set `binaryTargets` in schema.prisma** — always include `binaryTargets = ["native", "debian-openssl-3.0.x"]` in the Prisma generator block so Prisma generates the correct engine binary for the Debian container environment.
- **Copy full node_modules in the runner stage** — do NOT selectively copy `node_modules/.prisma`, `node_modules/prisma`, `node_modules/@prisma` separately. Instead use `COPY --from=builder --chown=nextjs:nodejs /app/node_modules ./node_modules` to copy everything. Prisma v6+ CLI has deep transitive deps (`@prisma/config` → `effect`, `c12`, etc.) that break if not all present.

